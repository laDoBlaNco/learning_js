// 1. You can't define a var more than once
let petName = 'Rocky';
petName = 'Spot';

console.log(petName);

// 2. There are rules related to var names
let test_ = 2;
let result = 3 + 4;

// 3. Variable names cannot be reserved keywords
// let let = 12 // this won't work