const isRaining = true;
// isRaining = false;
console.log(isRaining);

const person= {
    age: 46
}

person.age = 23
// person = {} // we can't change person to something else but we can change properties of a const person

console.log(person);

